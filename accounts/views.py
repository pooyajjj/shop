from django.shortcuts import render
from .models import CustomUser, OTP


"""
we need login, register and delete acounts(deacive) and confirmations accounts and prutch spatial accounts(limited), refererr 

"""
